<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item dropdown">
            <%= image_tag "https://source.unsplash.com/boE2xft0cAo/400x400", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu" aria-labelledby="navbarDropdown">
              <% if user_signed_in? %>
              <%= link_to "My account", user_path(current_user), class: "dropdown-item" %>
                <% end %>
              <%= link_to "Trees", trees_path, class: "dropdown-item" %>
              <%= link_to "Home", root_path, class: "dropdown-item" %>
            </div>
          </li>
        </ul>
      </div>
      <div class="d-flex text-right">
        <% if user_signed_in? %>
        <%= link_to "logout", destroy_user_session_path, method: :delete, class: "btn-ghost" %>
        <% else %>
        <%= link_to "Login", new_user_session_path, class: "btn-ghost" %>
        <% end %>
      </div>
 </div>
